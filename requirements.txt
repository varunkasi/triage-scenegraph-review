# Pinned to versions verified on vlm-image-review (2026-05-11).
# Transitive deps are listed for full reproducibility; install order doesn't matter.
# Tested Python: 3.12.3 (Ubuntu 24.04 system python).

# Direct deps (imported by server.py)
Flask==3.1.3
pillow==12.2.0

# Transitive (pulled in by Flask)
blinker==1.9.0
click==8.3.3
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
Werkzeug==3.1.8
